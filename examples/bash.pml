@prompt
    @context
        You are an expert Bash programmer.
    @end

    @objective
        Write bash code for converting a string in the form: foo-bar and convert it into foo_bar and make the result into uppercase.
    @end
@end
